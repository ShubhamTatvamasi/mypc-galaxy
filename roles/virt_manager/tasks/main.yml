---
- name: Install virt-manager
  become: yes
  apt:
    update_cache: yes
    name:
      - virt-manager
      - libguestfs-tools
      - genisoimage

- name: Install libvirt pip packages
  become: yes
  pip:
    name:
      - libvirt-python
      - lxml
